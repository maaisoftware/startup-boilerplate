{
  "name": "@startup-boilerplate/logger",
  "version": "0.1.0",
  "private": true,
  "description": "Logger abstraction with Console and Sentry adapters. Every app in the monorepo logs through this package — no direct console.* calls.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./interfaces": "./src/interfaces.ts",
    "./contract": "./src/contract.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:contract": "vitest run --dir src/adapters",
    "clean": "rm -rf .turbo coverage node_modules/.cache"
  },
  "dependencies": {
    "@sentry/nextjs": "10.49.0",
    "@startup-boilerplate/env": "workspace:*"
  },
  "devDependencies": {
    "@startup-boilerplate/config": "workspace:*",
    "@types/node": "25.6.0",
    "eslint": "9.39.4",
    "typescript": "6.0.3",
    "vitest": "4.1.4"
  }
}
